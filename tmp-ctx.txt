    if (_euMaxKm > 0 && _oppgittKm > _euMaxKm * 2) {
      const _ratio = (_oppgittKm / _euMaxKm).toFixed(1);
      log(`[km-mistanke] ${regnr}: oppgitt ${_oppgittKm} >> EU ${_euMaxKm} (faktor ${_ratio}) — beholder kunde-oppgitt (ingen auto-rette)`);
      sendTelegram(`\u26a0\ufe0f [km-mistanke] ${regnr}: kunde-oppgitt ${_oppgittKm} km >> EU-km ${_euMaxKm} (faktor ${_ratio}) — mulig typo, beholder kunde-oppgitt`).catch(function(){});
    }

    // 2-5. V2 prisemotor (erstatter Finn-scrape + gammelt anker)
    const seg = identifySegment(bil.model_year || vegData.firstRegYear || 0, bil.mileage || 0);
